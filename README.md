# pwa demo

Requirements
-----------
* [NodeJS](http://nodejs.org/) >= v12.x.
* [npm](https://www.npmjs.com/) >= v6.x.

Run
-----------
```

$ npm install --save-dev gulp

$ npm install

$ gulp
```

First instalation
-----------
```
$ npm install -g gulp

$ npm install -save-dev gulp

$ npm install

$ gulp
```

Dependencies list
-----------
```
"autoprefixer"
"browser-sync"
"cssnano"
"del"
"gulp"
"gulp-autoprefixer"
"gulp-imagemin"
"gulp-postcss"
"gulp-rename"
"gulp-sass"
"gulp-uglify"
"imagemin-mozjpeg"
"node-bourbon"
```
